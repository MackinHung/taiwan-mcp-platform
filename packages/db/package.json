{
  "name": "@mcp-platform/db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "migrate:local": "wrangler d1 execute mcp-platform --local --file=schema.sql",
    "seed:local": "wrangler d1 execute mcp-platform --local --file=seed.sql"
  }
}
