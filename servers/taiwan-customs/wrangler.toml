name = "taiwan-customs-mcp"
main = "src/index.ts"
compatibility_date = "2025-03-01"
compatibility_flags = ["nodejs_compat"]

[vars]
SERVER_NAME = "taiwan-customs"
SERVER_VERSION = "1.0.0"
# No API key needed — all endpoints are public open data
